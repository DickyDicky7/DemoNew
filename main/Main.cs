using Godot;

public class Main : Spatial
{
    public override void _Process(float delta)
    {
                    base._Process(      delta);

        //Input.ParseInputEvent(new InputEventAction() { Action = "U", Pressed = true, Strength = 1, });
    }
}
